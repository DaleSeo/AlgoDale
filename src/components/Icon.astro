---
import { Icon } from "astro-icon";

interface Props {
  name: string;
  size?: number;
  class?: string;
}

const { class: className, name, size = 24 } = Astro.props;
---

<Icon class={className} name={name} width={size} height={size} />
